var React = require('react')

// This is ES6 syntax
class App extends React.Component {
    // renders UI
    render () {
        return (
            // This is JSX not HTML
            <div>
                Hello React Training!
            </div>
        )

    }
}